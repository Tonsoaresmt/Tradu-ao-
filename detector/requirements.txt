# Detecção de balões + OCR. Instale num venv:
#   cd tradutor/detector
#   python -m venv .venv && .venv\Scripts\activate   (Windows)
#   pip install -r requirements.txt
#
# Core de detecção de balões. torch (CPU) entra junto do ultralytics.
# Para GPU NVIDIA (mais rápido), veja README: instalar torch do índice cu121.
ultralytics>=8.1.0
huggingface_hub>=0.20.0
Pillow>=10.0.0
pytesseract>=0.3.10   # OCR 'tesseract' — inglês->PT-BR (precisa do binário Tesseract instalado)
manga-ocr>=0.1.11     # OCR 'manga-ocr' — japonês (baixa transformers + modelo ~400MB na 1a vez)
